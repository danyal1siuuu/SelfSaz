SelfSaz V2.2 - Update Only

This package contains only the files changed for V2.2 relative to the supplied SelfSaz-main project.

Replace:
  SelfSaz-main/host_bot/bot.py

Do not rename the file or flatten the folder structure.
No __pycache__ or .pyc files are included.

The V2.2 bot.py contains the 175-admin-feature panel and the updated phone-login flow.
QR login behavior remains manual; no automatic QR refresh was added here.
